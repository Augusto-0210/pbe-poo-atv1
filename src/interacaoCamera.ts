//Importar a classe que quer utilizar
import { Camera } from './Camera';

//Instanciar (criar objeto)
const Camera1: Camera = new Camera('Preta','Pequena','Sony');
const Camera2: Camera = new Camera('Prata','Média','Nikon');